"""DualCore — a grounded dual-agent coding system.

Agent A (Coder) writes code, Agent B (Test Designer) writes an executable test
suite, and a sandbox actually runs both — so the refine loop is driven by real
pass/fail results instead of a second model's opinion.
"""

__version__ = "0.2.0"
